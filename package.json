{
  "scripts": {
    "build": "esbuild src/*.js src/*.css --bundle --outdir=dist",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@google/genai": "^1.27.0",
    "@google/generative-ai": "^0.24.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
